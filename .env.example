# CockroachDB Serverless connection string.
# Cloud console -> your cluster -> "Connect" -> "General connection string".
# It looks like the line below. Keep the ?sslmode=verify-full (or sslmode=require).
DATABASE_URL="postgresql://USER:PASSWORD@HOST.cockroachlabs.cloud:26257/DBNAME?sslmode=verify-full"
